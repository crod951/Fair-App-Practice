import styled from 'styled-components';

const CardContainer = styled.section`
	display: flex;
	flex-direction: row;
	flex-wrap: wrap;
	max-width: 1250px;
	margin: 20px auto;
	padding: 0 0 65px;
`;

export default CardContainer;
